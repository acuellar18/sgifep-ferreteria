# URL del login del Sistema para los enlaces "Acceso al sistema".
# En producción por defecto se usa "/login.html" (mismo origen).
# En desarrollo, si no se define, los enlaces apuntan a http://localhost:5174/login.html
#VITE_SISTEMA_LOGIN_URL=http://localhost:5174/login.html