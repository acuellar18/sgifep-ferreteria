# Variables de entorno del frontend (Vite), opcionales.
# Todas deben empezar con VITE_ para ser expuestas al navegador.

# Base de la API. En desarrollo se usa el proxy de Vite hacia el backend,
# por lo que normalmente NO hace falta cambiarla.
VITE_API_BASE=/api